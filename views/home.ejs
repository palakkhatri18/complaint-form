<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complaint Logger</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #434546;
            color: #000;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .form-container {
            background-color: #000;
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            max-width: 500px;
            width: 100%;
        }

        h1 {
            color: #17a2b8;
            text-align: center;
            font-family: 'hand-crafted', cursive;
        }

        .form-label {
            color: #17a2b8;
            text-align: left;
            margin-bottom: 0.5rem;
        }

        .form-control {
            background-color: #333;
            border: 1px solid #444;
            color: #fff;
            transition: box-shadow 0.3s ease, border-color 0.3s ease;
        }

        .form-control::placeholder {
            color: #aaa;
        }

        .form-control:focus,
        .form-control:hover {
            border-color: #80bdff;
            box-shadow: 0 0 8px rgba(0, 123, 255, 0.5);
        }

        .btn-warning {
            background-color: #ffc107;
            border-color: #ffc107;
            transition: background-color 0.3s ease, border-color 0.3s ease;
            color: #000;
        }

        .btn-warning:hover {
            background-color: #16e000;
            border-color: #ffffff;
        }

        .btn-primary {
            transition: background-color 0.3s ease, border-color 0.3s ease;
            color: #fff;
        }

        .btn-primary:hover {
            border-color: #00e000;
        }
    </style>
</head>

<body>
    <div class="form-container text-center">
        <h1 class="mb-3">IT Complaint Logger</h1>
       
        <form action="" method="post" enctype="multipart/form-data">
            <div class="mb-3 text-start">
                <label for="email" class="form-label">Email</label>
                <input type="email" id="email" placeholder="Enter Email" name="user_email" class="form-control"
                    required>
            </div>
            <div class="mb-3 text-start">
                <label for="name" class="form-label">Name</label>
                <input type="text" id="name" placeholder="Enter Name" name="user_name" class="form-control"
                    required>
            </div>
            <div class="mb-3 text-start">
                <label for="location" class="form-label">Location</label>
                <input type="text" id="location"
                    placeholder="Enter Location, Room No, Building and Seat No" name="user_location"
                    class="form-control" required>
            </div>
            <div class="mb-3 text-start">
                <label for="message" class="form-label">Message</label>
                <input type="text" id="message" placeholder="Enter Message" name="user_message"
                    class="form-control" required>
            </div>
            <div class="mb-3 text-start">
                <label for="file" class="form-label">Upload File</label>
                <input type="file" id="file" class="form-control" name="uploaded_file">
            </div>
            <button class="btn btn-warning mt-3" type="submit">Submit</button><br><br>
            <a href="/login" class="btn btn-primary me-2">Login</a>
        <a href="/signup" class="btn btn-success">Sign Up</a>
        <% if(isAuthenticated){ %>
            <a href="logout" class="btn btn-warning mt-2">Logout</a>
      
            <% } %>
        </form>
    </div>

    

    <script src="https://cdn.jsdelivr.net
